import { FullScreenCode, Main, Section, SectionInverted } from 'gatsby-mdx-theme'
import { Appear } from 'mdx-deck'

export default Section

# full text `Section` #1

## Install

1. In root of repo, run `yarn link`
2. In this folder, run `yarn link gatsby-mdx-theme`
3. Run `yarn start` to see theme

---

export default Section

# full text `Section` #2

1. In root of repo, run `yarn link`
2. In root of repo, run `yarn link`
3. Run `yarn start` to see theme
4. Run `yarn start` to see theme
5. Run `yarn start` to see theme

---

export default Section

# full text `Section` #3

## Install

- In root of repo, run `yarn link`
- In this folder, run `yarn link gatsby-mdx-theme`
- Run `yarn start` to see theme

---

# Appear

<ul>
  <Appear>
    <li>Run yarn start to see theme</li>
    <li>Run yarn start to see theme</li>
  </Appear>
</ul>

<!-- TODO: let md li automatically wrapped by Appear -->